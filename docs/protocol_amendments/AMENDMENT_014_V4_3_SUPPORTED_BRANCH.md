# Amendment 014 — v4.3 Supported-Branch Research Program

**Date:** 2026-08-21  
**State:** active development authorization; no new confirmation authorized

V4.2 is permanently closed as `SUPPORTED` for the preregistered Burgers scalar-curvature comparison. This amendment opens the result-dependent `SUPPORTED` branch of `SAEPS Master Research Program v4.0` without changing any v2, v3.6, v4.1 or v4.2 lock, raw record, adjudication or claim.

The next mandatory experiment is an external scalar replication on the already retained Allen--Cahn candidate. Development seeds are fixed to `70--74`; untouched confirmation seeds `75--84` are reserved but inactive until a separate protocol-plus-executable lock and clean preflight authorize them. Development may address forward stability, common-center availability, exact finite-gamma reduced curvature, curvature-specific solver validity, gamma-matched nonlinear profiling and directional-HVP implementation. It may not select settings using comparative `D`, SAEPS accuracy or figure appearance.

Later seed namespaces are reserved as follows:

- controlled-mechanism development `85--89`, confirmation `90--99`;
- two-parameter development `100--104`, confirmation `105--114`;
- scalability checkpoints `120--124`;
- paired noise/sparsity seeds `130--134` and paired architecture seeds `135--139`.

Reserved confirmation seeds remain unreadable for development decisions. Every future confirmation requires its own immutable config, executable hashes, semantic gate graph, integration tests, clean preflight and one-shot authorization.

The external-replication primary gold standard is exact finite-`gamma` reduced curvature. Gamma-matched nonlinear profiles are a separately reported bridge to the original nonlinear state-reoptimization claim; profile failure must not be silently converted into exact-Hessian validation. The frozen first-order GN adequacy indicator remains secondary and may not be recalibrated on confirmation data.

