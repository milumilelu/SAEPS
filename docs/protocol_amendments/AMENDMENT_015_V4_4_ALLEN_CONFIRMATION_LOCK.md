# Amendment 015 — v4.4 Allen--Cahn External Confirmation Lock

**Date:** 2026-08-21  
**State:** protocol/executable lock candidate; execution not authorized

Frozen Allen--Cahn held-out development passed the complete binding curvature chain 2/2. This amendment creates a separate untouched external-replication confirmation on seeds 75--84. It does not alter or rerun any Burgers, v3.6, v4.2 or Allen--Cahn development result.

The primary estimand is paired `D=E_raw-E_SAEPS` against exact finite-gamma reduced curvature. `SUPPORTED` requires at least 8 valid pairs, at least 8/10 planned strict wins, positive median D and a one-sided exact sign-test p-value at most 0.05. Invalid planned seeds are retained as planned non-wins.

Gamma-matched nonlinear profiles are an independently statused, nonbinding bridge to the original nonlinear-profile claim. The first-order GN indicator remains frozen and secondary. Neither may be recalibrated from confirmation data.

Execution requires a byte-locked runner and aggregator, raw-schema integration tests, semantic graph hash, clean zero-prior-run preflight and a separate one-shot authorization record.

