# Amendment 013 — v4.2 Corrected Untouched Confirmation Lock

**Date:** 2026-08-20  
**State:** protocol and executable lock; execution not yet authorized

V4.1 passed its frozen held-out full-chain gate 5/5. This amendment creates a separate v4.2 confirmation on untouched seeds 55--69. It does not correct, replace or rerun v3.6.

The v3.6 scientific definitions and four-part primary rule remain unchanged. V4.2 changes only the previously invalid execution semantics: parameter-curvature and score RHS have independent statuses, score is nonbinding, and all computable quantities are retained.

The config, runner, aggregator, separated numerical references, tests and semantic gate graph must be hash-locked before authorization. V4.2 may run exactly once after a clean preflight and separate authorization record.

