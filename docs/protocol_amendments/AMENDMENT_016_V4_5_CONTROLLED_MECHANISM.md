# Amendment 016 — v4.5 Controlled-Mechanism Closure

**Date:** 2026-08-21  
**State:** development authorization only; confirmation remains inactive

V4.4 is permanently closed as `SUPPORTED` for the locked Allen--Cahn comparative curvature endpoint. This amendment activates the next ordered node of the v4 supported branch without modifying any historical lock, raw record, result or claim.

The controlled manufactured parabolic benchmark, truth, Fourier library, selected source pair, source normalization, alpha grid, gamma rule and scientific thresholds remain those of the immutable P2 lock. The purpose is to test whether the planned-denominator mechanism result closes on fresh seeds after replacing the old first-order-only checkpoint screen with an auditable common state-local-minimum workflow. No setting may be selected using eta, monotonicity, Spearman correlation or figure appearance.

Development seeds are fixed to `85--89`: seeds `85--87` are engineering integration and seeds `88--89` are held out until a committed executable freeze. Development may compare only the inherited baseline center with the already registered deterministic enhanced center workflow, using center validity, state RMSE marked validation-only, numerical solver validity and cost. Threshold relaxation is forbidden.

Untouched confirmation seeds `90--99` are reserved and inactive. A later confirmation requires its own immutable config and executable hashes, semantic graph, actual raw-schema integration test, clean preflight and one-shot authorization. The future scientific gate remains strict: all ten planned centers must be valid, at least eight planned seeds must be monotonic across the five locked alpha values, and the median valid-seed Spearman coefficient must be at least `0.9`. Invalid seeds remain planned failures; seeds cannot be replaced or selectively rerun.
