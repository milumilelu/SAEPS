# Amendment 007 — v3.2 gamma-matched primary chain

The user authorized seed-20 development that promotes the gamma-matched nonlinear profile to the primary SAEPS validation chain. The unregularized profile remains mandatory but nonbinding and answers only whether the `gamma -> 0` geometry has a stable limit.

This amendment also preregisters independent sign-wise continuation branches, nominal/strict optimization-accuracy convergence, unconditional seed-20 CG/PCG diagnostics, exact gamma-matched Hessian reduction and the four-way primary comparison. Seeds 21–24 and 30–44 remain inactive; confirmation is not authorized.
