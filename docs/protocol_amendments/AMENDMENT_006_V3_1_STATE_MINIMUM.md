# Amendment 006 — v3.1 state-local-minimum development

The user authorized a development-only seed-20 workflow that must establish a true numerical state local minimum before curvature comparison. This amendment creates an isolated v3.1 namespace, aligns center/profile gradient tolerances, adds exact-Hessian and negative-direction gates, adds exact-Hessian trust-region saddle escape, and adds a Jacobi-PCG development gate.

Seeds 21–24 and 30–44 remain inactive. Passing seed 20 cannot authorize confirmation. A future lock requires at least 4/5 full-chain-valid development seeds and zero numerical solver failures.
