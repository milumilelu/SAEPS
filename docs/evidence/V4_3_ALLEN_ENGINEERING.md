# v4.3 Allen--Cahn Engineering Development

**Status:** `PASSED` — development engineering only
**Seeds / selected width:** `[70, 71, 72]` / `8`

Binding-valid chains: `3/3`. Parameter-only reference, curvature solver, exact finite-gamma gold and finite-primary nodes pass 3/3.

The directional-HVP correction matches the explicit correction on `3/3` seeds. Score diagnostics fail on `3/3` but remain nonbinding.

The gamma-matched nonlinear-profile bridge passes `1/3` and fails `2/3`; this limitation is retained and is not allowed to invalidate or validate the exact-curvature primary chain.

No comparative D/E/eta quantity was used for architecture or engineering selection. Confirmation remains unauthorized.
