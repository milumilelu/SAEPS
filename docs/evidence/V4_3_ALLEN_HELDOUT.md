# v4.3 Allen--Cahn Held-Out Development

**Status:** `PASSED` — frozen development executable

Seeds `[73, 74]` are binding-valid `2/2`; directional-HVP agreement passes `2/2`.

Score diagnostics fail `2/2` and remain nonbinding. The gamma-matched nonlinear-profile bridge passes `1/2`.

No D/E/eta quantity entered the held-out acceptance. This result permits drafting a separate confirmation lock but does not authorize seeds 75--84.
