# E0–E3 执行报告

日期：2026-09-16（本版为 E2 措辞更正版）。
证据标签：`jcp-submission-v1` = `d5a231d857e410b96ae66174fb98fec9c8b9b34a`。
证据与环境哈希：见 `output_manifest.json`。

本版**替代**此前仅覆盖 E0–E2 的说明，并更正其中对 E2 的过度解读。

## 一、按验证范围拆分的状态

不应把全部工作压缩成一个"通过"标记。按实际验证范围：

| 工作 | 根据本轮可以支持的判断 | 不能扩大为 |
|---|---|---|
| E0 历史数字与设定核查 | 论文数字、配置覆盖和计时口径得到复核 | 新的准确性确认 |
| E1 双参数自动微分核验 | 10 个真实存档点上的坐标恒等式得到验证 | 所有标量、双参数中心均已独立验证 |
| E1 标量块级核对 | 21 个归档矩阵记录内部自洽 | 原始残差与参数梯度已独立重算 |
| E2 原定标量队列 | 因原始张量缺失，尚未执行 | 已由双参数队列替代完成 |
| E2 双参数事后分析 | 精修可执行；已测出参照漂移量 | 参照已经稳定，或参照已经失效 |
| E3 预检 | 源项、周期边界、解析导数、非仿射恒等式全部通过 | 方法在非仿射参数上已获收益 |
| E3 开发队列 | 反演可收敛；精确约化在 α=1e-8 下不可用 | 已具备与主结果对比的条件 |

**本轮主要补强的是证据可信度与机制解释，不是方法适用范围的新确认。** 代数有效性、驻点有效性和方法收益三者没有合并。

## 二、E2 措辞更正（重要）

上一版报告写了：

> ~~"小梯度确实不蕴含小状态误差。"~~

**该表述过度解读，现予更正。** 理由：

1. E2 计算的是

   \[
   \Delta\theta_N=-(H_{\theta\theta}+\gamma I)^{-1}g_{\gamma,\theta},
   \]

   这是**局部二次模型建议的修正量**，不是实测的最优状态误差。把它当作真实误差界需要额外条件。
2. 可以构造二次目标，使其梯度很小、牛顿步很大，而所有 Hessian 块为常数。此时状态精修会移动权重，但 **Schur 参照完全不变**。所以"牛顿步大"不能推出"参照不可靠"。
3. "完整梯度"在本报告中一律改称**完整惩罚状态梯度**，表示 \(g_\theta+\gamma(\theta-\theta_0)\)，不是状态与物理参数的联合梯度。

**更正后的表述：**

> 较小的归一化状态梯度，与较大的局部牛顿修正量同时出现。仅凭梯度阈值，尚不能确认参照稳定性。

那么参照到底稳不稳？本轮已用第三节的漂移表直接测量，答案见表。

## 三、E2 曲率漂移（本轮新增的关键结果）

产出：`outputs/posthoc/e2_curvature/`。

### 3.1 目标与尺度

\[
L_\gamma(\theta,\lambda_0)=\tfrac12\|\bar r(\theta,\lambda_0)\|^2+\tfrac\gamma2\|\theta-\theta_0\|^2
\]

- 锚点 \(\theta_0\)、参数 \(\lambda_0\) 全部固定；精修期间**不重新锚定**。
- \(\gamma\) 在一级算定后**固定不变**，不随精修重新估计谱尺度。
- 目标全程采用**总和**尺度，损失、梯度、阻尼同尺度；因此不存在"均值损失配总和阻尼"导致的 736 倍相对阻尼放大。
- 漂移比较**固定锚点白化度量**；误差指标另按历史定义（逐级重建度量）一并给出，避免把换尺子当成漂移。

### 3.2 \(\gamma\) 来源

对 8 个历史有效点，直接读取历史诊断 \(\gamma\)。本轮在存档状态上复算的 \(\gamma\) 与历史值相对差为 \(1.0\times10^{-16}\sim3.6\times10^{-15}\)，即逐位一致。

对 **seed 219、221 不存在历史诊断 \(\gamma\)**：冻结代码 `src/saeps/v5/two_parameter_frozen.py:281` 只在 `center_valid` 分支内计算 \(\gamma\) 并生成约化参照。这两点的 \(\gamma\) 因此标为 **`new_posthoc_sensitivity_target`**——按同一冻结规则在存档状态上新算的**事后敏感性目标**，不称作恢复历史参照。

### 3.3 矩阵一致性（对照历史矩阵）

8 个有效点 × 3 个量 = 24 行，产出 `e2_matrix_consistency.csv`：

| 量 | 与历史记录的最大相对误差 |
|---|---|
| `F_raw` | **0.0**（逐位一致） |
| `F_se_GN_explicit` | 4.1e-13 |
| `H_red_exact_gamma` | **0.0**（逐位一致） |

23 个量的最大相对误差为 **2.27e-11**。这说明本适配器复现的不只是点位，还包括历史记录的三个约化矩阵本身。点位一致而权重或配置错误的情况，由此被排除。

### 3.4 漂移结果

精修按**嵌套迭代预算**（0/25/100/400/1000）从同一锚点出发，因此快照 k 就是完整运行第 k 次 L-BFGS 迭代后的状态。终点（1000 次）：

| seed | 有效 | ‖Δθ‖ | drift H_red | E_raw2 | E_SAEPS2 | drift / E_SAEPS2 |
|---|---|---|---|---|---|---|
| 218 | 是 | 7.1e-08 | 2.61e-06 | 14.60 | 0.02019 | 1.29e-04 |
| 224 | 是 | 1.9e-07 | 2.07e-05 | 12.34 | 0.09525 | 2.17e-04 |
| 215 | 是 | 9.4e-06 | 4.78e-05 | 17.36 | 0.02589 | 1.85e-03 |
| 220 | 是 | 1.1e-04 | 6.03e-05 | 9.238 | 0.03856 | 1.56e-03 |
| 222 | 是 | 9.6e-05 | 2.33e-04 | 14.96 | 0.02996 | 7.79e-03 |
| 216 | 是 | 6.0e-04 | 2.50e-04 | 9.575 | 0.02149 | 1.16e-02 |
| 223 | 是 | 1.2e-02 | 4.48e-03 | 15.49 | 0.1220 | 3.67e-02 |
| 217 | 是 | 3.9e-03 | 8.01e-04 | 14.41 | 0.01158 | 6.92e-02 |
| 219 | 否 | 1.1e-02 | 1.52e-02 | 17.33 | 0.05957 | 2.54e-01 |
| 221 | 否 | 3.8e-04 | 4.12e-04 | 10.48 | 0.007139 | 5.78e-02 |

**结论。** 在 8 个有效中心上，参照漂移为原 SAEPS 误差的 **0.013% – 6.9%**，没有任何有效中心达到同一量级（阈值 0.1 未被触及）。同时：

- **方法排序在全部 10 个中心、全部 5 个级别上均未改变**（SAEPS 始终优于 raw 对照同一级参照）。
- 未出现"用初始 SAEPS 配精修后参照"的错配统计。

对应到四种情形，本轮落在第 1、2 类：**可以报告"在已测试精修范围内参照稳定"，比较结论保留，绝对误差数字需附敏感性说明。** 本轮**不**支持"参照已经失效"，也不支持无条件"参照已证明稳定"。

### 3.5 达标率偏低的解释

此前报告 1e-8 达标 2/10、1e-10 达标 1/10。补充说明：三个级别各自从锚点重启且共享 1000 次上限，而 L-BFGS 在到达预算前按自身准则终止（实际 497–962 次迭代），因此三级轨迹相同、结论由**验收阈值**给出而非三次独立优化。达到停止条件不等于达到本实验的梯度阈值。

## 四、E1 的两条边界（保留）

### 4.1 恒等式成立不要求驻点

对 \(\bar r(\theta,p)=c(\theta)+B(\theta)p\) 有 \(H_{pp}=G_{pp}\)；对数坐标下 \(H_{\lambda\lambda}-G_{\lambda\lambda}=\operatorname{diag}(g_\lambda)\)。两条关系都不要求状态驻点，也不要求参数驻点。

因此 **seed 219、221 通过 E1 与其历史无效标签不冲突**；它们验证的是代数关系，**不得**据此进入原有效精度队列。

### 4.2 参数块没有 GN 截断，不代表约化 GN 已经精确

即使 \(H_{pp}=G_{pp}\)，仍可能有 \(H_{\theta\theta}\neq G_{\theta\theta}\)、\(H_{\theta p}\neq G_{\theta p}\)。状态消元后精确 Schur 参照与 SAEPS 仍可能不同——这正是原稿保留"松弛校正误差"的原因。

**E1 不推翻 SAEPS 的改善结果。** 它说明现有算例的固定状态截断项受参数结构与坐标选择的共同约束。

## 五、E3：非仿射物理参数

### 5.1 预检全部通过

产出：`outputs/development/e3/e3_preflight.json`。

| 检查 | 结果 |
|---|---|
| 制造解 \(u_t,u_{xx}\) 的解析式 vs 自动微分 | 5.6e-17 / 4.4e-15 |
| 源项在真值处满足 PDE | **0.0** |
| 周期边界（值 / 一阶导数） | 0.0 / 2.2e-16 |
| 网络四个分支 解析 vs 自动微分 | ≤ 2.2e-16 |
| 制造解最小值 | 0.791（协议要求 ≥ 0.7） |
| 残差数 / 状态参数数 | 368 / 65 |

### 5.2 非仿射恒等式

\[
H_{\lambda\lambda}-G_{\lambda\lambda}-g_\lambda=\kappa^2\sum_i \bar r_i\,\partial_{\kappa\kappa}\bar r_i,
\qquad
\partial_{\kappa\kappa}\bar r=-2\rho\frac{u^3}{(1+\kappa u)^3}
\]

在真值 \(\kappa^\star=1.2\) 处：

| 量 | 值 |
|---|---|
| 仿射预测 \(g_\lambda\) | **+38.739** |
| \(H_{\lambda\lambda}-G_{\lambda\lambda}\) | **−3.797** |
| 左端 \(H_{\lambda\lambda}-G_{\lambda\lambda}-g_\lambda\) | **−42.536345669803254** |
| 右端 \(\kappa^2\sum \bar r\,\partial_{\kappa\kappa}\bar r\) | **−42.536345669803254** |
| 绝对差 | **0.0** |

**这一个算例就清楚分开了两类贡献**：对数坐标产生的梯度项是 \(g_\lambda=+38.74\)，真正的物理参数非仿射项是 \(-42.54\)，二者量级相当，符号相反。也就是说 E3 的参数块**不再满足 E1 的恒等式**，偏离不是小扰动。该判定基于拟定模型的直接推导，本轮已数值验证两端相等。

### 5.3 开发队列结果（8 个拟合）

产出：`outputs/development/e3/e3_all_fits.csv`。

2 个数据种子 × 2 个初始化 × 2 档噪声 = 8 个开发拟合，按协议未扩大队列。

- **反演收敛良好**：\(\kappa\) 估计 1.171 – 1.219（真值 1.2），相对误差 **0.30% – 2.41%**。
- **状态的归一化惩罚梯度** 6.9e-6 – 2.1e-4。
- 然而 **8/8 在 \(\alpha=10^{-8}\) 下精确约化失败**，状态为 `REFERENCE_REDUCTION_FAILED`：

| | 最小状态特征值 | 正定容差 | 非正特征值个数 |
|---|---|---|---|
| 示例中心 A | −0.2651 | 5.5e-06 | 15 |
| 示例中心 B | −0.0121 | 4.7e-06 | 10 |

即 \(\gamma=\alpha\lambda_{\max}(J_\theta^\top J_\theta)\approx5.5\times10^{-4}\) 不足以把状态块抬为正定。

**处理方式：没有裁剪、没有加大阻尼偷换目标、没有强求 profile。** 按协议"状态块不正定时，不能强行求 profile"，如实记录为开发阶段未解决项。

这正是开发队列的用途：**在冻结协议与运行留出队列之前**，必须先解决这个问题（可选方向：为提高参照约化而采用更大阻尼——但需明确它对应不同几何，不能仅以误差更低来选；或加强状态精修使状态块真正正定）。**按结果挑选中心、阻尼或阈值都是禁止的。**

## 六、缺口

1. **标量中心无状态张量、观测点与残差。** E1 标量侧只能做块级自洽核对；E2 原定标量队列（Burgers 55/60/69，Allen–Cahn 75/79/84）为 `NOT_AVAILABLE`。
2. **原始标量确认张量未存档。** `outputs/runs/v5/checkpoints/burgers`、`allen_cahn` 为 `V5_RECONSTRUCTED_ENGINEERING_CHECKPOINT` 且 `historical_tensor_identity_claimed: false`，不能代替历史中心。
3. **seed 219、221 无历史诊断 \(\gamma\)**，已标为新定义的事后敏感性目标。
4. **E3 在 \(\alpha=10^{-8}\) 下无法产生精确参照**，留出队列与 E6/E7 因此暂不具备运行条件。
5. **原生峰值张量内存未记录**；RSS 不是替代。

## 七、未做

- 未训练任何 PINN 之外的新模型；未重跑、未替换任何历史种子。
- 未恢复用户在工作树中删除的文件。
- 未按结果重新挑选中心、阻尼或阈值。
- 未把事后分析改称预注册。
- 未写"参照已经失效"，也未写无条件"参照已证明稳定"。

## 八、复现命令

```bash
cd experiments/paper_revision_20260916
export PYTHONPATH="../../src:$PWD/src"

python -m pytest -q tests

python src/e0_audit.py            --out <dir>
python src/e1_identity.py         --out <dir>
python src/e2_stationarity.py     --out <dir>
python src/e2_curvature_drift.py  --out <dir>
python src/e3_saturation.py --verify --out <dir>/e3_preflight.json
python src/e3_saturation.py --develop --out <dir>
python src/manifest_outputs.py
```

所有脚本拒绝覆盖既有输出目录。
