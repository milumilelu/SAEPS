protocol_id: SAEPS-PAPER-REVISION-COVERAGE-SURROGATE-V1
status: DEVELOPMENT_ONLY
classification: POSTHOC_LOCAL_GAUSSIAN_COVERAGE_SURROGATE
purpose: feasibility_screen_before_end_to_end_monte_carlo_refitting
historical_outputs_immutable: true
new_training: false
source: outputs/posthoc/exact_fixed_state_v3
nominal_levels: [0.90, 0.95, 0.99]
claim_boundary: >-
  This is an analytic local-Gaussian surrogate, not empirical coverage. It assumes
  unbiased scalar estimation with H_red_exact as the local true information and
  therefore cannot replace repeated noisy-data refitting.
